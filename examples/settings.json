{
  "_comment": "Minimal Claude Code settings.json snippet to wire up claude-statusline.",
  "env": {
    "_comment": "FORCE_HYPERLINK is required for Windows Terminal — Claude Code's auto-detect list doesn't include WT_SESSION.",
    "FORCE_HYPERLINK": "1"
  },
  "statusLine": {
    "type": "command",
    "command": "node ~/.claude/statusline.js"
  }
}
